## Decision: Structured YAML with prompt assembly at runtime

The new `router_prompt.yaml` uses structured data (dicts/lists) for departments instead of free-text prompt sections. At load time, `load_router_prompt()` parses the structured YAML into a `RouterPromptConfig` dataclass. At runtime, `RouterPromptBuilder.build_response_create()` assembles the final prompt string from the structured config — combining system mechanics, identity, department routing rules, guardrails, and language instruction into a single `instructions` field.

This means the YAML is **data**, not a prompt template. The prompt text is generated by Python code from the structured data, making it possible to swap the YAML source for an API response without changing runtime behavior.

## Decision: Departments drive both prompt and tool definition

Each department entry in the YAML contains `description`, `triggers`, and `tool`. From this single source:

1. **Prompt generation**: The builder iterates departments to produce the routing rules section (replacing the old `decision_rules` + `departments` duplication).
2. **Tool definition**: `ROUTE_TOOL_DEFINITION` is generated dynamically — the `department` enum is built from the YAML keys. No more hardcoded enum list.
3. **Department enum**: The Python `Department` enum is validated against (or replaced by) the YAML department keys at load time.
4. **Coordinator dispatch**: The coordinator reads `dept_config.tool` to resolve the specialist tool name instead of `f"specialist_{department}"`.

## Decision: Keep Department as a runtime-validated set, not a static enum

Replace the static `Department(str, Enum)` with a set of valid department names loaded from YAML. The `parse_function_call_action` function validates against this set. This avoids the need to update Python code when departments change via API.

Alternative considered: Keep the static enum and validate YAML against it. Rejected because it defeats the purpose of dynamic configuration — adding a department would still require a code change.

## Decision: system_mechanic section is generated, not stored in YAML

The tool-calling mechanics ("ALWAYS call route_to_specialist", "never vocalize function names") are fixed behavior tied to the two-step routing architecture. Storing them in YAML implies they're configurable, but they aren't. The builder generates this text as a constant, keeping the YAML focused on what's actually configurable.

## Decision: Guardrails as a list, not free text

Guardrails become a YAML list of strings. The builder joins them with newline-prefixed bullets when assembling the prompt. This makes it easy to add/remove individual guardrails via API without parsing free text.

## Architecture

```
router_prompt.yaml (structured data)
        │
        ▼
load_router_prompt() → RouterPromptConfig (dataclass)
        │
        ├── identity: str
        ├── departments: dict[str, DepartmentConfig]
        │       ├── description: str
        │       ├── triggers: list[str]
        │       └── tool: str | None  (None for "direct")
        ├── guardrails: list[str]
        └── language_instruction: str
        │
        ▼
RouterPromptBuilder (runtime)
        │
        ├── build_response_create(history) → response.create payload
        │       ├── assembles prompt from config sections
        │       └── generates ROUTE_TOOL_DEFINITION dynamically
        │
        └── get_department_tool(department) → str | None
                └── used by Coordinator to resolve tool name

Coordinator
        │
        └── on model_router_action:
                tool_name = builder.get_department_tool(department)
                                    # instead of f"specialist_{department}"
```

## File changes

| File | Change |
|------|--------|
| `router_registry/v1/router_prompt.yaml` | Restructure from 5 text sections to structured format |
| `backend/src/routing/model_router.py` | Replace `RouterPromptTemplate` with `RouterPromptConfig` + `DepartmentConfig`. Replace static `ROUTE_TOOL_DEFINITION` and `Department` enum with dynamic generation. Add `get_department_tool()` method. |
| `backend/src/voice_runtime/coordinator.py` | Change specialist tool resolution from `f"specialist_{department}"` to `builder.get_department_tool(department)` |
| `backend/tests/unit/test_model_router.py` | Update for new YAML structure and dynamic generation |
| `backend/tests/unit/test_two_step_routing.py` | Update fixtures for new YAML structure |
| `backend/tests/e2e/test_event_pipeline.py` | Update fixtures if needed |
